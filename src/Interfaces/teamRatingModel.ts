export default interface teamRatingModel {
    id: string;
    teamNaim: string;
    score: number;
    winCount: number;
    loseCount: number;
    matchCount: number;
    lastMatch: string;
    futureMatch: string;
    imageId: string;
}